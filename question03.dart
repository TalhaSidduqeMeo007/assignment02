//Q.3: Create a list of Days and remove one by one from the end of list.



void main() {
  var weekdays = [
    'monday',
    'tuesday',
    'wednesday',
    'thursday',
    'friday',
    'saturday',
    'sunday'
  ];
  weekdays.removeLast();

  print(weekdays);
  weekdays.removeLast();
  print(weekdays);
  weekdays.removeLast();
  print(weekdays);
  weekdays.removeLast();
  print(weekdays);
  weekdays.removeLast();
  print(weekdays);
  weekdays.removeLast();
  print(weekdays);
  weekdays.removeLast();
  print(weekdays);
}
